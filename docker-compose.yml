services:
  mcp-memory-server:
    build: .
    container_name: mcp-memory-server
    ports:
      - "9321:9321"
    volumes:
      - ./data:/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  mcp-memory-server:
