services:
  mcp-memory-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-memory-server
    ports:
      - "9321:9321"
    volumes:
      - ./data:/data
    restart: unless-stopped

volumes:
  mcp-memory-server:
